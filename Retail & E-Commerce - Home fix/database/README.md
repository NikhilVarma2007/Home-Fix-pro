# Database

Run `schema.sql` in your Supabase SQL editor to create the `app_state` table.

The backend stores the whole app state as a single JSON document in Supabase, so the demo data stays aligned with the current UI.
