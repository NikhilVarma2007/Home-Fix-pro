# Python + FastAPI Backend

Run:

```bash
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload --host 127.0.0.1 --port 4000
```

This sample exposes the same backend route shape as the Node API.
